apiVersion: v1
kind: Service
metadata:
  name: ms-administrative-core
  namespace: qa
spec:
  selector:
    app: ms-administrative-core
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP